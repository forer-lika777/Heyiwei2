<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Heyiwei2.EditRecordForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Heyiwei2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="10" Width="300" >
        <TextBlock Text="选择记录年份："/>
        <ComboBox x:Name="RegionSelectComboBox"
                  SelectedIndex="0"
                  ItemsSource="{x:Bind Years}">
        </ComboBox>
        <TextBlock Text="选择记录月份："/>
        <ComboBox x:Name="BuildingSelectComboBox"
              SelectedIndex="0"
                  ItemsSource="{x:Bind Months}">
        </ComboBox>
        <TextBlock Text="输入用水量："/>
        <TextBox x:Name="DormNumberInput" PlaceholderText="例如：1"/>
        <TextBlock x:Name="OutputInfoTextBlock"/>
    </StackPanel>
</UserControl>
