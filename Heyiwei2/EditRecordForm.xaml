<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Heyiwei2.EditRecordForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Heyiwei2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="10"
                Width="300" >
        <TextBlock Text="选择记录年份："/>
        <ComboBox x:Name="YearSelectComboBox"
                  SelectedIndex="0"
                  SelectionChanged="YearSelectComboBox_SelectionChanged"
                  ItemsSource="{x:Bind Years}"/>
        <TextBlock Text="选择记录月份："/>
        <ComboBox x:Name="MonthSelectComboBox"
                  SelectedIndex="0"
                  ItemsSource="{x:Bind Months}"/>
        <TextBlock Text="输入用水量："/>
        <TextBox x:Name="UsageInput" 
                 TextChanged="UsageInput_TextChanged"
                 PlaceholderText="例如：1"/>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="共计消费："/>
            <TextBlock x:Name="CostTextBlock"/>
        </StackPanel>
        <CheckBox x:Name="HasPaidCheckBox"
                  Content="已缴费"/>
        <TextBlock x:Name="OutputInfoTextBlock"/>
    </StackPanel>
</UserControl>
